# n8n_repo

This repository is used by n8n Cursor Cloud Agents.

It provides a GitHub remote that Cursor Cloud Agents can clone and work against. Keep this branch (`n8n_main`) as the default so agent runs that target `n8n_main` succeed.
